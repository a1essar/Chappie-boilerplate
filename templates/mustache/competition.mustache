{{> head}}
{{> header}}
<main class="container">
    {{#list}}
    <div class="col-md-4">
        <div class="thumbnail">
            <img src="{{image}}" />
            <div class="caption">
                <h3>{{name}}</h3>
                <p>{{content}}</p>
                <p><a href="#{{index}}" class="btn btn-primary" role="button">Button</a> <a href="#{{index}}" class="btn btn-default" role="button">Button</a></p>
            </div>
        </div>
    </div>
    {{/list}}
</main>
{{> footer}}
